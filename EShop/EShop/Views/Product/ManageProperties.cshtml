@model IEnumerable<EShop.Models.ProductDetails>

@{
    ViewData["Title"] = "ManageProperties";
}

<h2>Product properties: product id is @ViewData["product_Id"]</h2>
<p>
    <a asp-action="AddProperty" asp-route-id=@ViewData["product_Id"]>Add new property</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Property)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Property)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <!--<a asp-action="DeleteProperty" asp-route-id="Model.Id">Delete Property</a> -->
                </td>
            </tr>
        }
    </tbody>
</table>