@model IEnumerable<EShop.Models.UserInRoleViewModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Manage users</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User.IsSuspended)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.User.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Role)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.IsSuspended)
                </td>
                <td>
                    <a asp-action="Delete" asp-route-id="@item.User.Id">Delete</a>

                    @if(!item.User.IsAdmin)
                    {
                        <a asp-action="GiveAdminPrivileges" asp-route-id="@item.User.Id">Make admin</a>
                    }
                    else
                    {
                        <a asp-action="RemoveAdminPrivileges" asp-route-id="@item.User.Id">Remove admin</a>
                    }                      
                    @if (!item.User.IsSuspended)
                    {
                        <a asp-action="Suspend" asp-route-id="@item.User.Id">Suspend</a>
                    }
                    else
                    {
                        <a asp-action="Restore" asp-route-id="@item.User.Id">Restore</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
